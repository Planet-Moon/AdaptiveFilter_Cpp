#include <string>
#include <iostream>
#include <json/json.h>

int main(int argc, char **argv){
    std::string line;
    while (std::getline(std::cin, line)) {
        // parse line
    }
    return 0;
}
